{% extends 'base.html.twig' %}

{% block title %}
    Devis & Réservation - Plage du midi
{% endblock %}

{% block header %}
    {{ component('Header', {'title' : page.title }) }}
{% endblock %}

{% block body %}
    <main id="reservation" class="main-reservation">
        <section>

            {{ form_start(form) }}

                <div class="begin-form">
                    <div class="container-name">

                        {{ form_row(form.name) }}
                        {{ form_row(form.firstName) }}

                    </div>    

                    {{ form_row(form.address) }}
                    {{ form_row(form.phone) }}

                    <div class="choice-quantity">

                        {{ form_row(form.nombreVoyageurs) }}
                        {{ form_row(form.nombreAnimaux) }}

                    </div>
                    <div class="container-card-voyageurs">

                        {{ form_row(form.ageDesVoyageurs) }}

                    </div>
                </div>
                <div class="end-form">
                    <div class="container-checkBox">
                        {{ form_row(form.chalet) }}
                        {{ form_row(form.chaletGite) }}
                        {{ form_row(form.cabane) }}
                        {{ form_row(form.roulotte) }}
                        {{ form_row(form.campingCar) }}
                        {{ form_row(form.caravane) }}
                        {{ form_row(form.tente) }}  
                    </div>
                    <div class="facultatif-container">
                        <p>Option</p>
                        {{ form_row(form.electricite) }}
                    </div>
                </div>

            {{ form_end(form) }}

        </section>
    </main>
{% endblock %}

{% block footer %}
    {{ component('Footer') }}
{% endblock %}